rios = {
    'nilo': 'egito',
    'solimos': 'brasil',
    'negro': 'brasil',
}

for rio, pais in rios.items():
    print('O {} corre pelo {}'.format(rio.title(), pais.title()))
    print('{}: {}'.format(rio.title(), pais.title()))
